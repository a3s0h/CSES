/*

    2^n -1 can  be written as (1<<n )-1 

    jitni bhi array ki length ha utte ki bit lo 
    like array -> 2,3,1

    toh bn skte ha 
    000    -> sb 0 ha tehrefore empty subsequence []
    001    -> last wala one ha tehrefore subsequence [1]
    010    -> [3]
    100    -> [2]
    and so on 

    that is jha jha 1 mil rha ha unka subsequence

*/





#include<bits/stdc++.h>
using namespace std ;

#define int long long
const int mod = 1e9+7;

signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int t ;
    cin>>t;
    while(t--)
    {
        cout<<"hello";
    }

    return 0;
}